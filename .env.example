# Copy this file to `.env.local` (or `.env`) and fill in values before running the app.
# Do NOT commit your real .env file or any secret keys.

# Public (client-safe) Supabase values
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional server-only key (never expose in the browser). Use only in server code and store
# it in a secure server-side env (do NOT prefix with NEXT_PUBLIC)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Example:
# NEXT_PUBLIC_SUPABASE_URL=https://xyzcompany.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI...

# only if ada domain/site
NEXT_PUBLIC_SITE_URL="www.domain.com"